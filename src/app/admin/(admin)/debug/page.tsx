import { DebugClient } from '@/components/debug/debug-client';

const DebugPage = () => {
    return (
        <div>
            <h1>Debug Page</h1>
            <DebugClient />
        </div>
    );
};

export default DebugPage;